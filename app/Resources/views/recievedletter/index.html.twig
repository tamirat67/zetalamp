{% extends 'home_page.html.twig' %}

{% block body %}

<div class="widget-box">
<div class="widget-header widget-header-flat widget-header-small">
<h5 class="widget-title">

<i class="ace-icon fa fa-download "></i>
Recieved Letters List
</h5>
<div class="widget-toolbar no-border">
													<div class="inline dropdown-hover">
														<button class="btn btn-minier btn-primary" style="margin-right: 35px;">
															Downloads
															<i class="ace-icon fa fa-angle-down icon-on-right bigger-110"></i>
														</button>
														<ul class="dropdown-menu dropdown-menu-right dropdown-125 dropdown-lighter dropdown-close dropdown-caret">
															<li class="active">
<a id="btnExport" class="pull-right" onclick="javascript:xport.toXLS('datatables-example');">
																<i class="ace-icon fa fa-caret-right bigger-110 invisible">&nbsp;</i>
 <i class="ace-icon fa fa-share "></i>														
 	Export Pdf
																</a>
															</li>
															<li class="active">
 <a href="#">
																
 <i class="ace-icon fa fa-download "></i>
 		Download Pdf
																</a>
															</li>
													
</ul>
</div>
</div>
</div>
</div>    
    
  <table id="datatables-example" class="table table-bordered table-striped table-condensed" style="font-size:11px; background:darkgray">    
 
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Ref_number</th>
                <th>Subject</th>
                                <th>Upload Letter</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for recievedLetter in recievedLetters %}
            <tr>
                <td><a href="{{ path('recievedletter_show', { 'id': recievedLetter.id }) }}">{{ recievedLetter.id }}</a></td>
                <td>{% if recievedLetter.date %}{{ recievedLetter.date|date('Y-m-d') }}{% endif %}</td>
                <td>{{ recievedLetter.refnumber }}</td>
                <td>{{ recievedLetter.subject }}</td>
                <td> <a href="{{ asset('/uploads/hojjatoota/' ~ recievedLetter.uploadletter) }}">View letter (PDF)</a>
                 </td>
                                   
                <td>
                
          <div class="input-group column col-sm-4 col-xs-4 sidebar-offcanvas">
                                    <div class="input-group-btn" >
                                        <a class="btn btn-sm btn-info" href="{{ path('recievedletter_show', { 'id': recievedLetter.id }) }}">
                                            <i class="ace-icon fa fa-eye-slash"></i>
</a>

                                        <a class="btn btn-sm btn-warning" href="{{ path('recievedletter_edit', { 'id': recievedLetter.id }) }}">
                                            <i class="ace-icon fa fa-pencil"></i>
</a>

                                 

                                    </div>
                                </div>  
                
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('recievedletter_new') }}">Create a new recievedLetter</a>
        </li>
    </ul>
{% endblock %}
